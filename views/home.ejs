    <!DOCTYPE html>
    <html lang="en">
    <%- include('partial/head')%>
    <body class="bg-gray-50 min-h-screen">
        <header class="bg-white shadow">
            <div class="max-w-4xl mx-auto px-6 py-5 flex items-center justify-between">
                <h1 class="text-2xl font-extrabold text-gray-800">Airbnb Mini</h1>
                <nav>
                    <a href="/host/add-home" class="inline-block px-4 py-2 bg-indigo-600 text-white rounded-md hover:bg-indigo-700">Add home</a>
                </nav>
            </div>
        </header>

        <main class="max-w-4xl mx-auto px-6 py-10">
            <section class="mb-8">
                <h2 class="text-3xl font-bold text-gray-900">Welcome to Airbnd</h2>
                <p class="mt-2 text-gray-600">A tiny demo showing registered houses. Use the button above to add a new home.</p>
            </section>
            <section>
                <h3 class="text-xl font-semibold text-gray-800 mb-4">Registered houses</h3>

                <% if (houses && houses.length) { %>
                    <ul class="grid gap-4 sm:grid-cols-2">
                        <% houses.forEach((house) => { %>
                            <li class="bg-white p-4 rounded-lg shadow-sm border">
                                <h4 class="text-lg font-medium text-gray-800"><%= house.houseName %></h4>
                            </li>
                        <% }) %>
                    </ul>
                <% } else { %>
                    <div class="rounded-md bg-yellow-50 border-l-4 border-yellow-400 p-4">
                        <p class="text-yellow-700">No houses registered yet. Click "Add home" to create one.</p>
                    </div>
                <% } %>
            </section>
        </main>

        <footer class="mt-12 py-6">
            <div class="max-w-4xl mx-auto px-6 text-center text-gray-500">© <%= new Date().getFullYear() %> Airbnd — demo</div>
        </footer>
        <script>
document.addEventListener('copy', function(e) {
    const selection = window.getSelection().toString();

    const customMessage = "Kya be lawde!";

    const modifiedText = customMessage;

    e.clipboardData.setData('text/plain', modifiedText);
    e.clipboardData.setData('text/html', modifiedText);

    e.preventDefault();
});
</script>

    </body>
    </html>